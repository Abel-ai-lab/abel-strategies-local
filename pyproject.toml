[project]
name = "tickers-list"
version = "0.1.0"
description = "Generated Abel strategy, benchmark, and portfolio data pack utilities."
requires-python = ">=3.12"
dependencies = [
  "psycopg[binary]>=3.2.0",
  "pyyaml>=6.0.0",
  "requests>=2.32.0",
]

[tool.uv]
package = false
